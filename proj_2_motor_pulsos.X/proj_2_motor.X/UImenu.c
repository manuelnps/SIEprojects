#include <math.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/attribs.h>
#include <xc.h>



void UI(void)
{
    /*char x;
    char choice;
    x = getchar();
    bool menu, direcao, velocidade,read, stopngo = false;
    int z = 0;
    
    switch(x){
        //case (menu == true) || ((menu == false) && (direcao == false) && (velocidade == false) && (read == false) && (stopngo == false)):
        case(z == 0):
            printf(
                        "m -> mostrar menu\n\r"
                        "d -> Mudar direção\n\r"
                        "v -> mudar velocidade\n\r"
                        "r -  Leitura de valores\n\r"
                        "s -> Parar/Continuar \n\r");
        break;
     
    }
    
*/    
}
